{
  "extends": "./tsconfig.json",
  "include": [ "./src/*.ts" ],
  "compilerOptions": {
    "noUnusedLocals": false, // don't fail build because of unused local variables.
    "noUnusedParameters": false, //  don't fail build because of unused parameters in functions
  }
}
